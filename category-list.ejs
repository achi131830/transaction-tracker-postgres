<!DOCTYPE html>
<html lang="zh-Hant">
<head>
  <meta charset="UTF-8">
  <title><%= category %> 的交易明細</title>
  <style>
    body { font-family: Arial, sans-serif; padding: 30px; }
    table { border-collapse: collapse; margin-top: 20px; }
    th, td { padding: 8px 12px; border: 1px solid #ccc; }
  </style>
</head>
<body>
  <h1>📋 類別：<%= category %> 的交易明細</h1>

  <% if (start && end) { %>
    <p>查詢範圍：<%= start %> 到 <%= end %></p>
  <% } else { %>
    <p>查詢範圍：全部日期</p>
  <% } %>

  <% if (transactions.length > 0) { %>
    <table>
      <thead>
        <tr>
          <th>編號</th>
          <th>交易日期</th>
          <th>說明</th>
          <th>金額</th>
        </tr>
      </thead>
      <tbody>
        <% transactions.forEach(item => { %>
          <tr>
            <td><%= item.編號 %></td>
            <td><%= item.交易日期 %></td>
            <td><%= item.說明 %></td>
            <td><%= item.金額 %> 元</td>
          </tr>
        <% }) %>
      </tbody>
    </table>
  <% } else { %>
    <p>此分類在此區間內沒有交易。</p>
  <% } %>

  <% if (from === 'partner') { %>
  <p><a href="/partner-analysis?start=<%= start %>&end=<%= end %>">⬅ 回共同圖表</a></p>
<% } else { %>
  <p><a href="/analysis?start=<%= start %>&end=<%= end %>">⬅ 回圖表</a></p>
<% } %>
</body>
</html>

